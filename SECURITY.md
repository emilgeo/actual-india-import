# Security

## Handling of your data

This tool runs entirely on your machine. Statements are read from disk and
written back to disk (or pushed directly to your own Actual server with
`--push`). Nothing is sent anywhere else, and there is no telemetry.

